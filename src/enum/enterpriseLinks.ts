// 独立的企业链接本地存储键，避免与书签存储耦合
export enum ENTERPRISE_LINK_STORAGE_KEYS {
  HAS = 'has_enterprise_links',
  LINKS = 'enterprise_links',
  LOCKED = 'enterprise_link_locked',
  PASSWORD = 'enterprise_link_password',
}

